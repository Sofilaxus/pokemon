export type TTypes = {
    name: string;
};

export type TAllPokemon = {
    count: number;
    next: string;
    previous: string;
    results: {
        name: string;
        url: string;
    }[];
};

export type TType = {

}
